# Round 2 · critic bundle + unit (fresh) — bundle-cover REJECT · bundle-overview+listing REJECT · unit REJECT
Passed: zip = 31 files byte-identical to child builds; fillable/pptx counts; overview standards table = yamls; catalog = yamls; price math; closers name real next lesson; bridges hedged; kicker/chip/masthead/footer uniform on plans + covers; covers read as one series at 200px; overview pages clean.
bundle-cover: MAJOR "No prep" ×3 and "six lessons" ×3 (badge, sub, chips) → badge "COMPLETE UNIT" (or drop), sub becomes a hook ("Every AI job starts and ends with a human."); MINOR dead .list/.row CSS.
bundle listing: MAJOR description names no standards codes → "Standards:" line with the yaml codes (consider widening standards: to all 13 codes the lessons carry); MAJOR bundle Preview = cover + 2 overview pages, no student page; watermark clipped → append 3 watermarked sample pages from children, shorten the band text; MINOR "one a day for a week" → "one a day for six class periods"; "less than the price of four" → "four paid lessons"; INCLUDES add per-lesson file set line; UPLOAD_SHEET free-zip note; overview "full discussion script" → "discussion guide (three questions with answers)"; "Reading level pitched at grade 6" → "checked to the middle-school range"; sub-tomorrow example → Lesson 3; title slot 3 "Future Skills" → "No Student AI Accounts".
unit: MAJOR L4 and L6 never state human → AI → human (TG Why-this-lesson + closer sub) — add one sentence each; MINOR L2 deck kicker carries "· Lesson 2 of 6" suffix (others bare); lesson-plan section naming differs (Objectives / Standards Alignment / Differentiation layouts) → adopt L1 layout on all six; cover CSS drift (kicker margin 70 vs 96px; sub #FFF/600/40 vs #E8E8F5/700/42) → L1 values everywhere; closers "sold separately" → "(Lesson N — in the unit bundle, or sold separately)"; L5 plan needs a with-devices line.

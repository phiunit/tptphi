# Round 3 — Critic report: L2 `ai-training-data-crate` (2026-09-09)

Fresh critic, blind. Checked review PNGs, cover-200, PDFs (LP 2 / WS 2 / TG 3 = .page counts), zip = INCLUDES, .pptx 12 slides + notes, fillable 53 widgets = 53 write-ins (filled rasters), standards text vs registry, FK 4.8, minutes arithmetic, TG page refs.

## cover — SHIP IT (no regressions)
## lesson-plan — REJECT
1. MAJOR — `:114` Gap Report (5 min) asks for 10 ruled lines + a say-back exchange; 8–10 min for 30 students. Fix: retime Setup 2 / Serve 8 / Gap Report 7 / Fix 3 (= 20), mirror in deck chips.
2. MINOR — `:111` "both agree on one Fit box" contradicts `:108` "each on their OWN sheet" and slide 8 / WS Rule 3.
3. MINOR — `:128` Exit #3 phrasing differs from the printed item (see cross-cutting).
## worksheet (print + fillable) — REJECT
1. MAJOR — `:141–142` Exit #3 is two-part (partner's gap + the idea YOU added) on ONE 26px line; it is the SL.6.1-assessed item.
2. MAJOR (fillable) — `.addcard` fields 165×35.6pt; the TG's own exemplar card wraps to 3 lines and the third is clipped.
3. MINOR — `:67` "Your check marks are what we count up at the end" — nothing counts them.
4. MINOR — `:73–84` cards carry no language, yet request #3 needs Spanish and the key says "zero Spanish tracks".
## teacher-guide — REJECT
1. MAJOR — `:90` Exit #3 exemplar/acceptance models only the gap half; the "idea you added" half is unkeyed.
2. MINOR — `:97` "(documented in 2018 research)" attached to the training-photo cause; the research documented the error gap and benchmark skew, vendors' training sets were undisclosed.
## deck — REJECT
1. MAJOR — `slides.yaml:70` slide 11 item 3 (and item 2) differ from the worksheet text on the assessed item.
2. MINOR — `:58` flat student-visible claim about vendors' training photos.
3. MINOR — `:52` bullet 4 restates Rule 3 on the same slide.
4. MINOR — `:5` title subtitle 41 words with em-dash + parenthetical.
## listing — REJECT
1. MINOR — `product.yaml:29` warm-up description mentions a Spanish request the warm-up doesn't have (that is Tomás on the worksheet).
2. MINOR — `:31` "tick a Fit box" — British; student pages say "check".
3. MINOR — `:27` "six classmates' requests" — the sheet has six printed plus one live.

Cross-cutting: Exit Ticket #3 exists in three phrasings (WS two-part; LP + deck gap-only; TG keys gap-only) and the two-part version got one line. Secondary: writing load never timed against handwriting speed; `.addcard` boxes too short for a realistic card.

---
## Lead adjudication (2026-09-09)
ACCEPT all 16. Exit #3 canonical wording (verbatim everywhere): "One gap your partner spotted, and the idea YOU added to it." Exit #2 canonical: worksheet text as printed, copied to slide 11. Retime Frame block Setup 2 / Serve 8 / Gap Report 7 / Fix 3. `.addcard` min-height 72 → 90px (field ≈ 46pt, three lines of 10pt fit); p.2 must stay one page by trimming spacing only, never a write-in size. TG 2018 line: "higher error rates on darker skin (documented in 2018 research), most likely because the example photos leaned lighter." Slide 8 (`:58`): "Photo tools got darker skin wrong more — their example photos were mostly light-skinned, researchers found." Title subtitle → "An AI is a music app; its training data is the playlist. Today you'll BE the app." WS `:67` → "Your check marks are your evidence for the Gap Report." Playlist header → "The Playlist — All 12 Songs You Know (all in English)"; Fix-the-Playlist card format gains "· language". Listing: "or a song for grandma's birthday dinner"; "check a Fit box"; "six classmates' requests plus one live one".

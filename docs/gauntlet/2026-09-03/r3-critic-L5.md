# Round 3 — Critic report: L5 `ai-boring-work` (2026-09-09)

Fresh critic, blind. Inspected all 20 review PNGs, four src pages, slides.yaml notes, product.yaml, UPLOAD_SHEET, PDFs (LP 2 / WS 2 / Fillable 2 / TG 3 = .page counts = listing), .pptx (12 slides + notes), fillable filled+rasterised, cover-200, L1 reference cover/worksheet, registry text. Worksheet FK 4.9.

## cover — SHIP IT (no findings)
## lesson-plan — REJECT
1. MAJOR — `:121,:123` Defend It procedure inconsistent: Speakers "say the frame" from their own sheet while "Listener 1 marks Speaker 1's sheet" (same sheet, same time); exit "Swap sheets back" implies a swap that was never written. A sub cannot run the assessed SL.6.4 moment.
2. MINOR — `:121` Speaker 1 / Listener 1 never assigned.
3. MINOR — `:122` trio fallback 6 × (30 s + marking) ≈ 6 min in a 5-min sub-block.
Passing: blocks 7+10+15+8+5 = 45; sub-blocks sum; 5 verbatim lines; transitions; what-ifs; materials true; differentiation named; Support set still yields ≥2 per column; chips = yaml; hedged bridge; slide/TG refs correct.
## worksheet (print + fillable) — REJECT
1. MAJOR — `:158` exit-ticket "The job:" write-in ≈0.8 in wide (field width 59 pt); "my uncle's food truck" renders "my uncle's fo".
2. MAJOR — `:141,:147` same Defend It gap in student words; no Listener 1/2 assignment.
3. MINOR — `:76` KEEP HUMAN tail "Humans only." vs plan/slide "The person is the whole point."
Passing: FK 4.9; DOK mix; lines 24px, columns 208px, rows 34px; ≤45% blank; US spelling; no retired terms; key satisfiable in full and Support sets; fillable 41 fields, one per write-in, only the job field fails.
## teacher-guide — SHIP IT
1. MINOR — `:120` "in the Share-Out setup" — no block named Share-Out (it is Defend It); "share-out" also floats in slide 9 notes and TG p.2.
## deck — REJECT
1. MAJOR — `slides.yaml:74` slide 10 inherits the Defend It gap.
2. MINOR — `slides.yaml:20` slide-3 notes bridge adds an unhedged prediction ("the human parts become more of the job") absent from the plan.
3. MINOR — `slides.yaml:87` slide 12 "Every AI job runs human → AI → human" is a universal claim.
Passing: 12 slides, chips = plan, ≤6 bullets, no overflow, keys only in notes.
## listing — SHIP IT (no findings; 77 / 2355 chars verified, INCLUDES = zip, saves 30% true)

Cross-cutting root cause: Defend It sheet handoff + role assignment, fix identically in LP / WS / deck.

---
## Lead adjudication (2026-09-09)
ACCEPT all. Procedure decided (verbatim, adapt register per surface):
"Face the person across from you. Speaker 1 reads their claim out loud (30 seconds), then slides their sheet across. Listener 1 checks the three boxes and writes keep or fix (30 seconds), then slides it back. Switch: Speaker 2 reads, Listener 2 marks. Everyone ends with their own sheet." Exit transition: "You already have your own sheet — bottom of page 2." Trio fallback: "Trio: 20 seconds each, listeners mark the boxes only." KEEP HUMAN tail unified to "The person is the whole point." Slide 12: "A good AI job runs human → AI → human." Slide-3 notes bridge mirrors the plan sentence exactly. "Share-Out"/"share-out" → "Defend It" in TG (2) and slide 9 notes.

# Round 2 · critic L5 (fresh) — all six REJECT, all on small items; side-by-side: cover WIN, ws p1 WIN, lp/tg/deck TIE. A/B: all 6 picks favour the rebuilt pages.
MAJOR cover sub "AI takes over tasks, not whole jobs." unhedged → "AI takes tasks first, not whole jobs."
MAJOR share-out procedure: pair-level roles, per-sheet strip — a silent partner can collect three checks; "every student is scored" not guaranteed; 15 pairs → one odd pair, no what-if → "Each Speaker says the frame once (30 s each); Listener 1 marks Speaker 1's sheet, Listener 2 marks Speaker 2's"; what-if odd pairs → trio rotates; Pairs-of-pairs 5 / Whole-class 2. TG/deck/description "every student" → "each student's claim".
MAJOR "tick" (WS l.143, slide 10, TG ×10) → "check". MAJOR .blank 22px → 24px; "because ___" its own full-width line.
MINOR AUTOMATE panel adds "No decisions to make."; slide 12 "The people who win…" → hedge/drop; UPLOAD_SHEET stale (title, chars) → npm run sheet; Preview regenerated at the end.

# Copy to .env (never committed). Used only by scripts/upload_tpt.mjs on YOUR machine.
TPT_EMAIL=you@example.com
TPT_PASSWORD=change-me
# Optional: path to a Chromium/Chrome binary if playwright browsers aren't installed
CHROMIUM_PATH=
